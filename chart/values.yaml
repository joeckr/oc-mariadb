mariadb:
  name: mariadb
  app: mariadb
  replicaCount: 1
  resources: { limits: { cpu: 100m, memory: 200Mi } }
  image: ghcr.io/joeckr/mariadb
  tag: latest
  pullPolicy: Always
  port: 3306
  db: mariadb
  user: mariadb
  password: changeme
  rootPassword: really_replaceme
  storageAccessMode: ReadWriteOnce
  storageSize: 1Gi
  storageClass: ""

initSchema:
  enabled: true
